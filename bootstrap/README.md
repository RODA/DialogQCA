# DialogQCA Bootstrap

This folder registers DialogQCA as a product extension of the base app.

It should compose product metadata, QCA dialogs, QCA menus, QCA capabilities, product translations, and startup behavior.

It must not contain QCA feature logic. Put that in `capabilities`, `qca`, or `runtime-r`.
